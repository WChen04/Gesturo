👋 Gesturo Starter Project (No MediaPipe Needed)

✅ Uses OpenCV and NumPy to detect hand in real time using skin color + contours

📦 How to Run:
1. Open PowerShell
2. Navigate here: cd path\to\Gesturo-Starter
3. (Optional) Create a virtual environment:
   python -m venv venv
   .\venv\Scripts\Activate
4. Install dependencies:
   pip install opencv-python numpy
5. Run the program:
   python main.py

🧠 Move your hand into the blue box on the screen. The contour will be outlined in green.